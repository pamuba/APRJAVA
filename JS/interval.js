
// var setRef;

// setRef = setTimeout(
//      ()=>console.log('Timed Out'), 3000
// )
// // setInterval

// clearTimeout(setRef)

var setRef;
setRef = setInterval(
    ()=>console.log(new Date().getSeconds()), 1000
)
// clearInterval(setRef)

// console.log(new Date().getMinutes())

//Timer

// can you declare and define the setRef in one line